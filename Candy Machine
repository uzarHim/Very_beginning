import datetime

# Calculate time starting from 1 second and correctly rolling over minutes and hours
result['TIME'] = [datetime.time(((i+1) // 3600) % 24, ((i+1) // 60) % 60, (i+1) % 60) for i in range(len(df_commit))]
